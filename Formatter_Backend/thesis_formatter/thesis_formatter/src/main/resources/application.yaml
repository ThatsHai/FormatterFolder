spring:
  application:
    name: thesis_formatter
  datasource:
    url: "jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:Formatter}"
    password: ${DATABASE_PASSWORD:root}
    username: ${DATABASE_USER:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 100MB
      max-request-size: 115MB
server:
  port: 8080
jwt:
  signerKey: "kDnLkIQNLqJ4Bm2AoiGLRQwk4RUUsrfVrT7Q0I3uxMx+/ngrZnKnOeCtIXpcmUF6"
  valid-duration: 900 #in second 15minutes
  refeshable-duration: 172800 #in second 2 days
